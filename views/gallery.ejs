 <!DOCTYPE html>
  <html lang="en">
  <head><%- include('partials/head', { title: 'Gallery' }) %></head>
  <body>
    <div class="layout">
      <%- include('partials/nav') %>
      <main class="main-content">
        <header class="gallery-header">
          <h1>Gallery</h1>
          <span class="gallery-count"><%= artworks.length %> works</span>
        </header>
        <hr class="gallery-rule" />
        <div class="gallery-grid">
          <% artworks.forEach(artwork => { %>
            <a href="/artworks/<%= artwork.slug %>" class="gallery-card">
              <img src="/<%= artwork.imagePath %>" alt="<%= artwork.title %>" />
              <div class="card-meta">
                <div class="card-title"><%= artwork.title %></div>
                <div class="card-sub"><%= artwork.medium %> &mdash; <%= artwork.year %></div>
              </div>
            </a>
          <% }) %>
        </div>
      </main>
    </div>
  </body>
  </html>